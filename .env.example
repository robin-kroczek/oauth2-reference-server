# .env.example
# Kopieren nach .env:  cp .env.example .env

# Image, das Compose startet.
# Vorher lokal bauen mit:
#   docker build -t oauth2-reference-server:local .
APP_IMAGE=oauth2-reference-server:local